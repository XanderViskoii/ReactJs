import React from 'react';
import ReactDOM from 'react-dom/client';
//React core foundational library , ReactDOM is for web

import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
);

